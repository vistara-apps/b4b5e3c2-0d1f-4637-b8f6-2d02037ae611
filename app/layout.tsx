import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Guardiant - Your Pocket Rights Advisor',
  description: 'A mobile app for individuals to quickly access legal rights information during police interactions, record incidents discreetly, and share summarized information.',
  keywords: 'legal rights, police interactions, incident recording, civil rights',
  authors: [{ name: 'Guardiant Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
